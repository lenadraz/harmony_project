## Backend and ML Components

- **backend/** – Server-side application that manages API endpoints, handles data requests, and coordinates communication between the frontend and the machine learning service.

- **ml_service/** – Machine learning service responsible for generating embeddings, computing similarity scores, and producing match recommendations.
