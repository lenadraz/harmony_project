<template>
   <!-- Root application component.
       It only renders the current route using Vue Router.
       All actual pages (Login, Matches, Profile, etc.) are injected here via <router-view>. -->
  <router-view />
</template>
